# ARCHIVIERT

**Dieses Projekt ist archiviert. Es wird in [ibr-venus-services](https://github.com/ErwinRieger/ibr-venus-services) (als `dbus-ibr-loads`) weitergeführt.**

**dbus-ibr-loads**: Steuert einen Verbraucher (z.B. Wasserkocher) via MQTT, um überschüssige PV-Energie zu nutzen.

